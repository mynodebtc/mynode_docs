// Extends the bundled default theme, overriding only components/Navbar.vue
// (to show a shorter site name than the <title>/OG site title — see that
// file for why they need to differ).
module.exports = {
  extend: '@vuepress/theme-default'
}
